users:
  uid: {
  userName: string,
  adminFlag: boolean,
  createdAt: timestamp,
  updatedAt: timestamp
  }

books:
  id: {
    isbn: string,
    title: string,
    author: string,
    publicationDate: date,
    purchaseDate: date,
    price: number,
    versionNumber: number,
    imageLink: string,
    storageLocation: string,
    lendingStatus: boolean,
    createdAt: timestamp,
    updatedAt: timestamp
  }

storages:
  storageId: {
    storageLocation: string
  }

lending: {
  id: {
    uid: string,
    bookId: string,
    isbn: string,
    title: string,
    author: string,
    imageLink: string,
    checkoutDate: date,
    returnDate: date,
    storageLocation: string,
    createdAt: timestamp,
    updatedAt: timestamp
  }
}

history: {
  id: {
    uid: string,
    bookId: string,
    isbn: string,
    title: string,
    author: string,
    imageLink: string,
    checkoutDate: date,
    returnDate: date,
    createdAt: timestamp,
    updatedAt: timestamp,
  }
}